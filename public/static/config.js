// 应用全局配置文件使用用例
; (function (win) {
  // var pathName = win.location.pathname.split('/')[1]
  win.APP_CONFIG = {
    // BASE_URL: 'http://' + win.location.host + '/' + 'web' + '/'
    BASE_URL: 'http://defunction.cn/web/'
  }
})(window)